# نظام تقديم الطلبات الإلكترونية - دليل الإعداد

تم إنشاء نظام متكامل لتقديم ومتابعة الطلبات الإلكترونية بنجاح! ✅

## 📋 ما تم إنشاؤه:

### 1. **Models (4)**
- `Center` - المراكز
- `RequestType` - أنواع الطلبات
- `RequestStatus` - حالات الطلبات
- `Request` - الطلبات الرئيسية

### 2. **Migrations (4)**
- `create_centers_table`
- `create_request_types_table`
- `create_request_statuses_table`
- `create_requests_table`

### 3. **Controllers (5)**
- `CentersController` - إدارة المراكز
- `RequestTypesController` - إدارة أنواع الطلبات
- `RequestStatusesController` - إدارة حالات الطلبات
- `RequestsController` - إدارة الطلبات (لوحة التحكم)
- `PublicRequestsController` - واجهة المواطنين

### 4. **Views**
#### Dashboard Views:
- Centers: index, create, edit
- Request Types: index, create, edit
- Request Statuses: index, create, edit (مع حقل اللون)
- Requests: index

#### Public Views:
- `/submit-request` - نموذج تقديم طلب جديد
- `/request-success/{tracking_number}` - صفحة النجاح مع رقم التتبع
- `/track-request` - نموذج البحث عن طلب
- نتيجة التتبع - عرض حالة الطلب وتحميل الرد

### 5. **Routes**
- Routes عامة (بدون تسجيل دخول) لتقديم ومتابعة الطلبات
- Routes محمية للوحة التحكم مع الصلاحيات

### 6. **Seeders**
- `CenterSeeder` - 5 مراكز تجريبية
- `RequestTypeSeeder` - 6 أنواع طلبات (شكوى، تضامن، صحة، إسكان، توظيف، تعليم)
- `RequestStatusSeeder` - 5 حالات مع ألوان (جاري المراجعة، تحت الإجراء، تم التحويل، تم الرد، مرفوض)
- `RequestSeeder` - 3 طلبات تجريبية

### 7. **Permissions**
- `create_center`, `edit_center`, `delete_center`
- `create_request_type`, `edit_request_type`, `delete_request_type`
- `create_request_status`, `edit_request_status`, `delete_request_status`
- `create_request`, `edit_request`, `delete_request`

---

## 🚀 خطوات الإعداد:

### 1. تشغيل الـ Migrations:
```bash
php artisan migrate
```

### 2. تشغيل الـ Seeders:
```bash
php artisan db:seed
```
أو لتشغيل seeder معين:
```bash
php artisan db:seed --class=CenterSeeder
php artisan db:seed --class=RequestTypeSeeder
php artisan db:seed --class=RequestStatusSeeder
php artisan db:seed --class=RequestSeeder
```

### 3. إنشاء symbolic link للتخزين (إذا لم يكن موجوداً):
```bash
php artisan storage:link
```

### 4. التأكد من الصلاحيات:
- تسجيل الدخول كـ Super Admin
- الصلاحيات تم إضافتها تلقائياً للـ super_admin role

---

## 📍 الروابط المهمة:

### للمواطنين (Public):
- **تقديم طلب جديد**: `http://your-domain.com/submit-request`
- **تتبع طلب**: `http://your-domain.com/track-request`

### لوحة التحكم (Dashboard):
- **الطلبات**: `/requests`
- **المراكز**: `/centers`
- **أنواع الطلبات**: `/request-types`
- **حالات الطلبات**: `/request-statuses`

---

## ✨ المميزات:

### للمواطنين:
1. ✅ تقديم طلب جديد مع رفع مستندات
2. ✅ الحصول على رقم تتبع فوري
3. ✅ تتبع الطلب بـ 3 طرق:
   - رقم التتبع
   - الرقم القومي
   - رقم الهاتف
4. ✅ عرض حالة الطلب بالألوان
5. ✅ تحميل الرد المكتوب (إن وجد)
6. ✅ واجهة بسيطة وسهلة الاستخدام

### للوحة التحكم:
1. ✅ إدارة كاملة للطلبات (عرض، تعديل، حذف)
2. ✅ تحديث حالة الطلب
3. ✅ إضافة سبب الرفض
4. ✅ رفع رد مكتوب/إشعار
5. ✅ إدارة المراكز
6. ✅ إدارة أنواع الطلبات
7. ✅ إدارة حالات الطلبات مع الألوان
8. ✅ نظام صلاحيات محكم
9. ✅ Activity Logging
10. ✅ DataTables للبحث والفلترة

---

## 🔐 الصلاحيات:

يمكنك إنشاء أدوار مختلفة وإعطائها صلاحيات محددة:

**مثال:**
- **موظف استقبال**: `create_request`
- **مدير القسم**: `create_request`, `edit_request`
- **المدير العام**: جميع الصلاحيات

---

## 🎨 التخصيص:

### تغيير ألوان الحالات:
في لوحة التحكم -> حالات الطلبات -> تعديل -> اختر اللون

### إضافة مراكز جديدة:
لوحة التحكم -> المراكز -> إضافة مركز جديد

### إضافة أنواع طلبات جديدة:
لوحة التحكم -> أنواع الطلبات -> إضافة نوع جديد

---

## 📝 ملاحظات:

1. **رقم التتبع**: يتم توليده تلقائياً بالصيغة: `REQ-2025-XXXXXXXX`
2. **المستندات**: يتم قبول PDF وصور (JPG, PNG) بحد أقصى 5MB لكل ملف
3. **الرد المكتوب**: يمكن رفعه من صفحة تعديل الطلب في لوحة التحكم
4. **Soft Deletes**: جميع الحذف soft delete (يمكن استرجاع البيانات)

---

## 🔧 المتطلبات الإضافية:

تأكد من تثبيت الحزم التالية (يجب أن تكون مثبتة مسبقاً):
- `yajra/laravel-datatables`
- `spatie/laravel-permission`
- `spatie/laravel-activitylog`
- `maatwebsite/laravel-excel` (إذا أردت إضافة Excel import/export لاحقاً)

---

## 🎯 الخطوات التالية (اختيارية):

1. **إضافة إشعارات**: إرسال SMS/Email عند تغيير حالة الطلب
2. **تقارير**: إضافة صفحة تقارير بالإحصائيات
3. **Excel Export**: تصدير الطلبات إلى Excel
4. **Dashboard Cards**: إضافة بطاقات إحصائية في الداشبورد
5. **طباعة الطلب**: إضافة زر طباعة للطلب

---

**تم بناء النظام بنجاح! 🎉**

للاستفسارات أو المساعدة، راجع ملف `RESOURCE_STRUCTURE.md` للتعرف على البنية العامة.

